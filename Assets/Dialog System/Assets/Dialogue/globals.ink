VAR pokemon_name = ""

EXTERNAL playEmote(emoteName)
EXTERNAL newClearAreaQuest(questName, questDescription, questAmount, areaId)
EXTERNAL newGoToQuest(questName, questDescription, destinationId)
EXTERNAL newKillQuest(questName, questDescription, amount, destinationId)
EXTERNAL ending()

// Save System
// EXTERNAL saveStateDialogue()
// EXTERNAL saveOneWayDialogue(dialogue)